vich_uploader:
    db_driver: orm

    mappings:
        agenda_images:
            uri_prefix: '%app.path.agenda_images%'
            namer: Vich\UploaderBundle\Naming\UniqidNamer
            upload_destination: '%kernel.project_dir%/public%app.path.agenda_images%'
